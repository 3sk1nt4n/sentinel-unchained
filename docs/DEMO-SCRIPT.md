# Unchained - sub-three-minute demo script

> **Status:** RECORDING DRAFT. Record only from a retained authentic COMPLETE
> GPT-5.6 Sol bundle. Replace every placeholder from verified artifacts. Do not
> present the synthetic Docker log or capped `PARTIAL` bundle as the flagship
> completed investigation.

Target runtime: **2:45–2:50**. The current Official Rules require the video to
be shorter than three minutes, publicly visible on YouTube, include clear audio,
and cover what was built and how Codex and GPT-5.6 were used. The
[Official Rules](https://openai.devpost.com/rules) control.

## Required recording artifacts

- Exact final commit/tag: `PENDING_FINAL_FREEZE`.
- Evidence displayed only as public case name, `E001`, byte count, and SHA-256.
- Authentic COMPLETE bundle: `PENDING_COMPLETE_BUNDLE`.
- Strict-verifier output: `PENDING`.
- Automated viewer-QA output: `PENDING`.
- One authentic finding and exact supporting span: `PENDING`.
- One authentic reviewer transition: `PENDING`.

Never show an API key, key-file path, private filesystem path, raw request body,
or unredacted evidence-derived content that is not intended for publication.

## 0:00–0:18 - Problem and promise

### Screen

Repository hero, then the evidence folder card showing only:

```text
E001 · Windows memory · 2,147,483,648 bytes
SHA-256 8079a7459b1739caf7d4fbf6dde5eb0ae7a9d24dbde657debf4d5202c8dc6b62
```

### Narration

> Autonomous forensic agents are easy to demo and difficult to trust. Unchained
> separates model strategy from evidence authority. GPT-5.6 chooses where to
> look; deterministic code controls what may run and verifies exactly what was
> executed and cited.

## 0:18–0:38 - Deterministic evidence front door

### Screen

Run or replay the retained output of:

```powershell
sentinel onboard <PUBLIC_DEMO_EVIDENCE_DIRECTORY> --json
```

Highlight Windows, memory-only, `E001`, symbol readiness, SHA-256, capability
label, and `openai_called: false`.

### Narration

> Before any model call, Unchained identifies the evidence by content, assigns a
> public ID, checks symbol readiness, derives the legal tool route, and records
> initial SHA-256 custody. Local paths never become model authority.

## 0:38–1:05 - GPT-5.6 opening book and concurrent execution

### Screen

Use the authentic audit/progress view. Show GPT-5.6 choosing up to six typed
Volatility tools, followed by concurrent `tool.started` receipts and
content-addressed completions.

### Narration

> GPT-5.6 chooses the opening strategy. Code validates the complete batch
> all-or-none, reserves the caps, binds every call to E001 and its digest, then
> runs the valid tools concurrently. The model cannot choose a shell, command,
> executable, path, timeout, or cap.

## 1:05–1:28 - One inspectable adaptive transition

### Screen

Show one authentic bounded request card:

```text
PLAN -> one typed ACT -> retained OBSERVATION -> visible ledger UPDATE
```

### Narration

> Later turns are compact and stateless: the visible case ledger, deterministic
> receipt index, current budget, and only the latest relevant observations. Each
> turn can take one new forensic action, so the state transition stays auditable.

## 1:28–1:53 - Exact evidence span and monotonic review

### Screen

Show one authentic finding with:

- finding ID;
- investigator status;
- judge status;
- tool-call ID;
- retained artifact SHA-256;
- UTF-8 byte start/end;
- exact supporting text.

If the authentic reviewer downgraded a finding, show that transition. If it did
not, show a preserved finding and say it was preserved. Never manufacture a
downgrade for the video.

### Narration for a downgrade

> The investigator proposed this finding, but the fresh-context reviewer could
> only preserve or reduce it. It downgraded the status because the exact retained
> support was weaker than the claim. It cannot invent findings or upgrade them.

### Narration when preserved

> The fresh-context reviewer preserved this status from the exact resolved span.
> It could reduce the claim, but it could not invent a finding or upgrade it.

## 1:53–2:18 - Strict offline verification

### Screen

Run:

```powershell
sentinel verify <COMPLETE_BUNDLE> --require-complete --require-live-gpt56
python scripts/check_viewer.py <COMPLETE_BUNDLE> --require-complete --require-live-gpt56
```

### Narration

> Verification reconstructs the lifecycle, typed calls, budgets, exact evidence
> spans, status transitions, report, viewer, manifest, and recorded final custody.
> The viewer check reapplies an independent positive HTML policy and requires no
> network or server. This establishes local proof consistency and traceability;
> it does not claim to authenticate OpenAI or decide forensic truth.

## 2:18–2:40 - Judge-friendly proof viewer

### Screen

Open the authentic retained `viewer.html` through `sentinel view`. Scroll only
through:

1. evidence inventory and custody;
2. typed tool receipts;
3. findings and investigator-to-judge transitions;
4. evidence spans and limitations.

### Narration

> A judge can inspect the retained result without the evidence image, API key,
> Python environment, or running web service. The model writes bounded narrative;
> deterministic code owns the authoritative rows and citations.

## 2:40–2:50 - Close

### Screen

Return to the hero and display the final verified commit/tag.

### Narration

> Unchained is GPT-5.6 directing a bounded investigation whose actions, citations,
> custody, and final report can be checked independently. GPT-5.6 chooses where
> to look; code proves what ran and was cited.

Do not shorten the closing line to “code proves the incident.”


## Final recording gate

- [ ] Runtime is less than 2:55 before upload.
- [ ] Audio is clear at normal laptop volume.
- [ ] Every displayed metric exists in a committed or hosted retained artifact.
- [ ] No key, private path, hidden browser tab, or unrelated notification appears.
- [ ] The bundle shown is authentic and COMPLETE.
- [ ] Any replay is labeled as a replay of the retained authentic bundle.
- [ ] Viewer is readable at 1080p and hashes wrap cleanly.
- [ ] Video is public on YouTube.
- [ ] Final URL works in a signed-out browser.
